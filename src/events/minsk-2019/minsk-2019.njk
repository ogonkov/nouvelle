---
tags:
    - event
    - conf
layout: event.njk
date: 2019-12-07
location:
    city: Минск
    address: Октябрьская, 16А
    venue_name: SPACE
    venue_url: http://eventspace.by/
---

Программа

<ol>
    {% for item in collections.minsk_2019 %}
    <li>
        <time>{{ item.data.date | time }}</time>

        {% if item.data.title %}
            <div id="{{ item.fileSlug }}">
                <h3>{{ item.data.title | safe }}</h3>

                {{ item.templateContent | safe }}

                {% if item.data.speaker %}
                <p>
                    <a href="#{{ item.data.speaker | slug }}">
                        {{ item.data.speaker }}
                    </a>
                </p>
                {% endif %}
            </div>
        {% else %}
            {{ item.templateContent | safe }}
        {% endif %}
    </li>
    {% endfor %}
</ol>
